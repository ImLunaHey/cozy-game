// The size of each square in the grid
export const gridSize = 80;

// The size of the dirt square inside each grid square
export const dirtSize = 60;

// The size of the crop
export const cropSize = 20;
